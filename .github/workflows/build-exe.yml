name: Build Windows EXE

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Run Tests
        run: |
          python -m unittest discover tests

      - name: Build EXE with PyInstaller
        run: |
          python tools/optimize_assets.py
          pyinstaller AutoRewardsPC.spec --clean

      - name: Zip Release
        run: Compress-Archive -Path dist/AutoRewardsPC -DestinationPath dist/AutoRewardsPC.zip

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: AutoRewardsPC-Windows
          path: dist/AutoRewardsPC.zip
          if-no-files-found: error

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/AutoRewardsPC.zip
          generate_release_notes: true

      - name: Inspect dist folder
        run: Get-ChildItem dist -Recurse
